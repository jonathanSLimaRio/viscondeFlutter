import 'package:flutter/material.dart';

class GameBlankScreen extends StatelessWidget {
  const GameBlankScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
